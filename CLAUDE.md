@AGENTS.md

# Dev Notes
- Static export: `npm run build` → `/out` directory
- R3F quartet versions pinned in package.json (no ^ or ~)
- All component styles use CSS Modules
- Zustand store: `src/lib/store.ts`
- Design tokens: `src/styles/tokens.css`
